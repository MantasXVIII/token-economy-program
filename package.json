{
  "name": "token-economy",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install && node build.js"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "wrangler": "^3.22.0"
  }
}
